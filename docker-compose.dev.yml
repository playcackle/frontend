version: '3.8'

services:
  frontend-dev:
    build:
      context: .
      dockerfile: Dockerfile
      target: development
    ports:
      - "3000:3000"
    environment:
      - NODE_ENV=development
      - NEXT_TELEMETRY_DISABLED=1
      # Backend connection URLs for development
      - NEXT_PUBLIC_LOBBY_MANAGER_URL=http://localhost:8001
      - NEXT_PUBLIC_LOBBY_WS_BASE_URL=ws://localhost:8000
    volumes:
      # Mount source code for hot reloading
      - .:/app
      - /app/node_modules
      - /app/.next
    command: npm run dev
    restart: unless-stopped

networks:
  default:
    name: snapscore_frontend_dev
    driver: bridge